我的目标是编写一个GO服务器端，支持发送的本地 OpenAI 请求转发到 nvidia 服务器上，可以参考 e:\Projects\Trae\NvidiaAPI\test.py 发送请求。服务器支持多帐号，每帐号轮流发送请求，失败的帐号隔一段时间再进入可用帐号池，另外支持限流功能，也就是支持每分钟最多请求数（ nvidia 的限制是每分钟最多 40 个请求），以及突发过多请求时自动平滑处理，也就是最小发送时间间隔不能太短。这些都支持使用配置文件设置，服务器启动时自动读取。限流功能可以使用成熟的GO库。

项目开发需要遵守 AGENTS.md 中提到的所有规范。
项目开发使用 TDD 原则进行开发。