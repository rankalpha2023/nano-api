# 第一性原理开发规约：四段式沙漏架构

## 1. 系统四层结构 (The Four-Layer Rule)
所有的代码必须属于且仅属于以下四层之一：
- **Types 层**: 定义全局 Data Schema 和 Action Payload。严禁包含任何逻辑。
- **Core 层**: 纯函数计算（算法、转换、数学）。禁止副作用，禁止访问外部环境（Window/DB）。
- **Engine 层**: 状态管理中心。通过 接口 修改状态，编排副作用管道。
- **UI 层**: 纯净渲染。仅订阅数据和转发指令。禁止超过 3 行的逻辑计算。